file = open('Text1.txt', 'r')
for each in file :
    print(each)                 # This will print every line one by one in the file

#print(file.read(24))           #the interpreter will read the first five characters of stored data and return it as a string